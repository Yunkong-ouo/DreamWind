//══════════════════════    說明   ════════════════════════════════════
//在這個文件中, 您可以按照您的 *個人喜好* 自定義修改整個CFG初始設定
//按照註釋進行修改即可
//您不應該修改以 // 開頭的行！！
//您不應該修改以 // 開頭的行！！
//您不應該修改以 // 開頭的行！！
//您不應該修改以 // 開頭的行！！

//═════════════════════════════════════════════════════════════════════════════

//修改 "輪盤觸發鍵", 默認為 rshift (右Shift)
//  !!!受游戲限制, 此鍵不可以為 MOUSE1~MOUSE5!!!
bind rshift "+ClickGui" | grep ~
//修改 "CFG重置鍵", 用於任何情況下重置CFG至此文件的設定，默認為左斜杠鍵 (右shift鍵左側)
bind / "ResetCFG" | grep ~
//修改 "CFG返回主頁面鍵", 用於任何情況下返回CFG主頁面，默認為句號鍵 (問號鍵鍵左側)
bind . "+ResetGUIPage" | grep ~

//滑鼠
//左鍵
bind mouse1 "+MOUSE1Total" | grep ~
//右鍵
bind mouse2 "+attack2" | grep ~
//中鍵
bind mouse3 "player_ping" | grep ~
//上下移動
bind mouse_x "yaw" | grep ~
//左右移動
bind mouse_y "pitch" | grep ~
//上滾輪
bind mwheelup "MWheelUpTotal" | grep ~
//下滾輪
bind mwheeldown "MWheelDownTotal" | grep ~

//移動
//前進
bind w "+ForwardTotal" | grep ~
//後退
bind s "+BackTotal" | grep ~
//左
bind a "+LeftTotal" | grep ~
//右
bind d "+RightTotal" | grep ~
//跳躍
bind space "+SpaceTotal" | grep ~
//静步
bind shift "+ShiftTotal" | grep ~
//蹲
bind ctrl "+CtrlTotal" | grep ~

//武器
//主武器
bind 1 "+1Total" | grep ~
//副武器
bind 2 "+2Total" | grep ~
//刀
bind 3 "+3Total" | grep ~
//炸彈
bind 5 "+5Total" | grep ~

//道具
//輪盤切道具/切道具
bind 4 "+4Total" | grep ~
//手榴彈
bind key "+6Total" | grep ~
//閃光彈
bind 7 "+7Total" | grep ~
//煙霧彈
bind 6 "+8Total" | grep ~
//誘餌彈
bind key "+9Total" | grep ~
//燃燒彈
bind 8 "+10Total" | grep ~

//特殊功能
//選擇跳投
bind h "+JumpThrow" | grep ~
//跳投
bind v "+JumpThrow2" | grep ~
//大跳
bind key "+KeyDuckJump" | grep ~

//切換左右手持槍
bind key "switchhands" | grep ~
//切換雷達尺寸
bind capslock "toggleradarscale" | grep ~
//退回所有已購買物品
bind backspace "sellbackall" | grep ~

//═════════════════════════════════════════════════════════════════════════════

//在exec前面加 "//" 取消個人設定
exec "CS2Konc_CFG/config/PersonalSettings"

//═════════════════════════════════════════════════════════════════════════════
